import { useState } from "react";
import { MapContainer, TileLayer, Polyline, useMapEvents } from "react-leaflet";

const ClickablePolylines = ()=>{
    const [routes, setSoutes] = useState([
        {
            id:1,
            coordinates: [
                
            ]
        }
    ])
}